import frontSourceImages from './images/front-source-image';
import backSourceImages from './images/back-source-image';

export default {
    namespaced: true,
    modules: {
        frontSourceImages,
        backSourceImages,
    },
};

